<!DOCTYPE html>
<html>
  <head>
    <title>RPG Task App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet">

    <%= javascript_importmap_tags %>
  </head>

  <body style="
    background-image: url(<%= asset_path(timed_background_image) %>);
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    background-attachment: fixed;
  ">

    <!-- ▼ ナビゲーションバー（共通メニュー） ▼ -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
      <div class="container-fluid">
        <a class="navbar-brand" href="<%= root_path %>">RPG Task</a>

        <button class="navbar-toggler" type="button"
                data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">

          <ul class="navbar-nav me-auto">

            <li class="nav-item">
              <a class="nav-link" href="<%= root_path %>">ホーム</a>
            </li>

            <li class="nav-item">
              <a class="nav-link" href="<%= quests_path %>">クエスト一覧</a>
            </li>

            <!-- ▼ ここが追加された「マイページ」 ▼ -->
            <li class="nav-item">
              <a class="nav-link" href="<%= mypage_path %>">マイページ</a>
            </li>

          </ul>

          <ul class="navbar-nav">
            <li class="nav-item">
              <%= button_to "ログアウト",
                    destroy_user_session_path,
                    method: :delete,
                    class: "btn btn-outline-light btn-sm" %>
            </li>
          </ul>

        </div>
      </div>
    </nav>
    <!-- ▲ ナビゲーションバー ▲ -->

    <div class="container">
      <%= yield %>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  </body>
</html>
