<!DOCTYPE html>
<html>
  <head>
    <title>RPG Task App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- アプリCSS -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <!-- ドットフォント -->
    <link href="https://fonts.googleapis.com/css2?family=DotGothic16&display=swap" rel="stylesheet">

    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet">

    <%= javascript_importmap_tags %>
  </head>

  <!-- ★ inline style を完全撤去 -->
  <body style="font-family: 'DotGothic16', monospace;">

    <!-- ▼ ナビゲーションバー ▼ -->
    <nav class="navbar navbar-expand-lg mb-4" style="background-color:#000; border-bottom:2px solid #fff;">
      <div class="container-fluid">
        <a class="navbar-brand text-white" href="<%= root_path %>">RPG Task</a>

        <button class="navbar-toggler" type="button"
                data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">

          <ul class="navbar-nav me-auto">
            <li class="nav-item"><a class="nav-link text-white" href="<%= root_path %>">ホーム</a></li>
            <li class="nav-item"><a class="nav-link text-white" href="<%= quests_path %>">クエスト一覧</a></li>
            <li class="nav-item"><a class="nav-link text-white" href="<%= mypage_path %>">マイページ</a></li>
            <li class="nav-item"><a class="nav-link text-white" href="<%= login_calendar_path %>">ログインカレンダー</a></li>
          </ul>

          <ul class="navbar-nav">
            <li class="nav-item">
              <%= button_to "ログアウト",
                    destroy_user_session_path,
                    method: :delete,
                    class: "btn" %>
            </li>
          </ul>

        </div>
      </div>
    </nav>
    <!-- ▲ ナビゲーションバー ▲ -->

    <div class="container">
      <%= yield %>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
