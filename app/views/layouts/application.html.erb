<!DOCTYPE html>
<html>
  <head>
    <title>RPG Task App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <link href="https://fonts.googleapis.com/css2?family=DotGothic16&display=swap" rel="stylesheet">

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet">

    <%= javascript_importmap_tags %>
  </head>

  <body style="font-family: 'DotGothic16', monospace;">

    <nav class="navbar navbar-expand-lg mb-4"
         style="background-color:#000; border-bottom:2px solid #fff;">
      <div class="container-fluid">

        <!-- ★ ここをロゴ画像に変更 -->
        <%= link_to root_path, class: "navbar-brand d-flex align-items-center" do %>
          <%= image_tag "logo.png",
                alt: "RPG Task",
                style: "height: 40px;" %>
        <% end %>

        <button class="navbar-toggler" type="button"
                data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav me-auto">
            <!-- ★ ホームだけ Turbo 無効 -->
            <li class="nav-item">
              <a class="nav-link text-white"
                 href="<%= root_path %>"
                 data-turbo="false">
                ホーム
              </a>
            </li>

            <li class="nav-item">
              <a class="nav-link text-white" href="<%= quests_path %>">クエスト一覧</a>
            </li>

            <li class="nav-item">
              <a class="nav-link text-white" href="<%= mypage_path %>">マイページ</a>
            </li>
          </ul>

          <ul class="navbar-nav">
            <li class="nav-item">
              <%= button_to "ログアウト",
                    destroy_user_session_path,
                    method: :delete,
                    class: "btn" %>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <div class="container">
      <%= yield %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
